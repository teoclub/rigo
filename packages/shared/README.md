# @teoclub/shared workspace

Issue 003 workspace entry. Packages land here in later phases:

- shared: Shared Service Definitions consumed by domain packages.
- work: Rigo Work domain providers (bundle, knowledge, documents, actions, approval).
- code: Rigo Code domain providers (minimal repository + file read/write bundle).
- api: HTTP/SSE host and shared API surface.
- bundle: product bundle composition (work-base, code-base) and reference profiles.
