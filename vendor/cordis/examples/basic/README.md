# Basic example

Minimal programmatic Cordis application: one plugin, one effect, one event.

```sh
cd examples/basic
bun install   # or npm install
bun main.ts   # or: node --experimental-strip-types main.ts
```
